# 🚀 Инструкция по запуску KODLA

Следуйте этим шагам, чтобы развернуть проект локально.

## Предварительные требования
*   **Node.js** (v18 или выше)
*   **Docker** и **Docker Compose** (для Базы данных и Judge0)
*   **API Key** для Google Gemini (опционально, для работы AI)

---

## Шаг 1: Настройка Окружения

1.  В папке `server/` создайте файл `.env` на основе примера (или используйте предоставленный):
    ```env
    DATABASE_URL="postgresql://kodla_user:kodla_secure_password@localhost:5432/kodla_db?schema=public"
    JWT_SECRET="secret_key_change_me"
    PORT=8080
    GEMINI_API_KEY="your_google_ai_key"
    ```

## Шаг 2: Запуск Инфраструктуры (База данных)

Из корневой директории проекта запустите Docker Compose:

```bash
docker-compose up -d
```
*Это поднимет контейнер с PostgreSQL (порт 5432).*

## Шаг 3: Настройка Бэкенда (Server)

Откройте новый терминал и перейдите в папку сервера:

```bash
cd server
```

1.  **Установите зависимости:**
    ```bash
    npm install
    ```

2.  **Синхронизируйте схему БД:**
    ```bash
    npx prisma db push
    ```
    *Это создаст таблицы в PostgreSQL.*

3.  **Наполните БД данными (Seeding):**
    *Важно: Это создаст курсы (Go, Java), модули и задачи.*
    ```bash
    npm run seed
    ```

4.  **Запустите сервер:**
    ```bash
    npm run start:dev
    ```
    *API будет доступно по адресу: `http://localhost:8080`*

## Шаг 4: Настройка Фронтенда (Client)

Откройте новый терминал в корневой директории проекта (где лежит `package.json` фронтенда или `vite.config.ts`).

1.  **Установите зависимости:**
    ```bash
    npm install
    ```

2.  **Запустите клиент:**
    ```bash
    npm start
    # или, если используется Vite:
    npm run dev
    ```
    *Приложение откроется по адресу: `http://localhost:3000` (или 5173).*

---

## ✅ Проверка работоспособности

1.  Зайдите на фронтенд.
2.  Перейдите на страницу `/login`.
3.  Зарегистрируйте нового пользователя.
4.  Перейдите в "Courses" -> выберите "Go Language".
5.  Вы должны увидеть структуру курса, загруженную из БД.

## 🐛 Возможные проблемы

*   **Ошибка подключения к БД:** Убедитесь, что Docker контейнер `kodla_postgres` запущен (`docker ps`).
*   **AI не отвечает:** Проверьте наличие `GEMINI_API_KEY` в `.env` сервера.
*   **Код не выполняется:** Если Judge0 не запущен, бэкенд будет использовать Mock-режим (имитация выполнения). Для реального выполнения нужно развернуть `docker-compose.judge0.yml`.
