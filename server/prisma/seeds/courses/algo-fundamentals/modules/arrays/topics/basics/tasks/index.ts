import { task as twoSum } from './01-two-sum';
import { task as maxSubarray } from './02-max-subarray';
import { task as mergeSorted } from './03-merge-sorted';
import { task as slidingWindow } from './04-sliding-window';
import { task as prefixSum } from './05-prefix-sum';
import { task as rotateArray } from './06-rotate-array';

export const tasks = [
	twoSum,
	maxSubarray,
	mergeSorted,
	slidingWindow,
	prefixSum,
	rotateArray,
];
