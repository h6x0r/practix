import { task as binarySearch } from './01-binary-search';
import { task as searchInsertPosition } from './02-search-insert-position';
import { task as firstLastPosition } from './03-first-last-position';
import { task as findPeakElement } from './04-find-peak-element';
import { task as findMinimumRotated } from './05-find-minimum-rotated';
import { task as search2DMatrix } from './06-search-2d-matrix';

export const tasks = [
	binarySearch,
	searchInsertPosition,
	firstLastPosition,
	findPeakElement,
	findMinimumRotated,
	search2DMatrix,
];

export default tasks;
