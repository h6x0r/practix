# Test Coverage Audit Report

**Дата:** 2026-02-14  
**Всего задач:** ~1301 (22 курса)  
**Текущее покрытие E2E тестами:** 263 теста (Playwright)

---

## 1. Обзор проблем

### 1.1 Критические проблемы

| Проблема | Влияние | Приоритет |
|----------|---------|-----------|
| 168 задач с `language: "unknown"` | Тесты не запускаются | 🔴 Критично |
| Java test runner не поддерживает wildcard imports | Java задачи падают | 🔴 Критично |
| E2E тесты покрывают только 88 задач | 830 задач не тестируются | 🔴 Критично |

### 1.2 Задачи с неизвестным языком

| Курс | Всего задач | Unknown | Причина |
|------|-------------|---------|---------|
| python-ml-fundamentals | 88 | 63 | Теги без python |
| java-ml | 50 | 45 | Теги без java |
| java-nlp | 40 | 38 | Теги без java |
| python-llm | 31 | 19 | Теги без python |
| java-advanced | 68 | 3 | Теги без java |
| **ИТОГО** | 277 | **168** | |

---

## 2. Breakdown по языкам

| Язык | Количество задач | Статус |
|------|------------------|--------|
| Go | 316 | ✅ Работает |
| Java | 168 | ⚠️ Частично (wildcard imports) |
| Python | 221 | ✅ Работает |
| TypeScript | 44 | ✅ Работает |
| JavaScript | 1 | ✅ Работает |
| **Unknown** | **168** | ❌ Не тестируется |

---

## 3. План исправления

### Этап 1: Исправить extract-solutions.ts ✅
**Файл:** `e2e/scripts/extract-solutions.ts`

Проблема: `detectLanguage()` смотрит только на теги, не на courseSlug.

```typescript
// Было:
function detectLanguage(tags: string[]): string {
  const languageTags = ['python', 'go', 'java', ...];
  for (const tag of tags) {
    if (languageTags.includes(tag.toLowerCase())) return tag;
  }
  return 'unknown';
}

// Нужно добавить fallback на courseSlug:
function detectLanguage(tags: string[], courseSlug: string): string {
  // 1. Сначала проверяем теги
  // 2. Потом проверяем courseSlug (java-ml → java, python-llm → python)
  // 3. Только потом unknown
}
```

### Этап 2: Исправить Java test runner
**Файл:** `server/src/judge0/judge0.service.ts:743-900`

Проблема: Regex не обрабатывает wildcard imports типа `java.lang.reflect.*;`

```typescript
// Было (строка ~780):
.replace(/import\s+java\.\w+(\.\w+)*;/gm, "")

// Нужно:
.replace(/import\s+java\.\w+(\.\w+)*(\.\*)?;/gm, "")
```

### Этап 3: Регенерировать solutions.json
```bash
npm run e2e:extract-solutions
```

### Этап 4: Запустить полный E2E тест
```bash
E2E_TIER=FULL npx playwright test e2e/tests/task-validation/
```

---

## 4. Структура E2E тестов

### Текущие файлы
```
e2e/tests/task-validation/
├── go-tasks.spec.ts      # Go задачи
├── java-tasks.spec.ts    # Java задачи  
├── python-tasks.spec.ts  # Python задачи
└── js-tasks.spec.ts      # JavaScript задачи
```

### Тиры тестирования
| Тир | Задач на язык | Всего | Использование |
|-----|---------------|-------|---------------|
| QUICK | 20 | ~88 | По умолчанию, CI |
| DAILY | 250 | ~250 | Ежедневные проверки |
| FULL | ВСЕ | 918 | Релизы, глубокая проверка |

---

## 5. Checklist исправлений

- [ ] Исправить `detectLanguage()` в extract-solutions.ts
- [ ] Исправить Java wildcard import regex в judge0.service.ts
- [ ] Регенерировать solutions.json
- [ ] Проверить что 0 задач с `language: "unknown"`
- [ ] Запустить Go E2E тесты (E2E_TIER=FULL)
- [ ] Запустить Java E2E тесты (E2E_TIER=FULL)
- [ ] Запустить Python E2E тесты (E2E_TIER=FULL)
- [ ] Проверить UI отображение ошибок в Results tab
- [ ] Обновить этот документ с результатами

---

## 6. Результаты (заполнить после исправлений)

### Go Tasks
- Всего: 316
- Проходит: _
- Падает: _
- Причины падений: _

### Java Tasks
- Всего: 168
- Проходит: _
- Падает: _
- Причины падений: _

### Python Tasks
- Всего: 221
- Проходит: _
- Падает: _
- Причины падений: _

---

**Последнее обновление:** 2026-02-14
