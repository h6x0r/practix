# Practix Platform: Полное описание функционала

> Обновлено: 2026-02-14

---

## Содержание

1. [Обзор платформы](#обзор-платформы)
2. [Курсы и задачи](#курсы-и-задачи)
3. [Рабочее пространство задач](#рабочее-пространство-задач)
4. [Выполнение кода](#выполнение-кода)
5. [AI Tutor](#ai-tutor)
6. [Prompt Engineering задачи](#prompt-engineering-задачи)
7. [Roadmap Generator](#roadmap-generator)
8. [Playground (Web IDE)](#playground-web-ide)
9. [Система гамификации](#система-гамификации)
10. [Подписки и платежи](#подписки-и-платежи)
11. [Личный кабинет](#личный-кабинет)
12. [Админ панель](#админ-панель)
13. [Локализация](#локализация)
14. [Безопасность](#безопасность)
15. [Технический стек](#технический-стек)
16. [API Reference](#api-reference)

---

## Обзор платформы

**Practix** — полнофункциональная образовательная платформа для обучения программированию с интегрированным исполнителем кода, системой гамификации, AI тьютором и платёжной системой.

### Ключевые метрики

| Метрика | Значение |
|---------|----------|
| Курсов | 22 |
| Задач | ~1301 |
| Языков программирования | 8 |
| Языков интерфейса | 3 (EN/RU/UZ) |
| Модулей | 147 |
| Тем | 100+ |

### Поддерживаемые языки программирования

| Язык | Версия | Статус |
|------|--------|--------|
| Go | 1.x | ✅ Production |
| Java | JDK 11+ | ✅ Production |
| Python | 3.9+ | ✅ Production |
| JavaScript | Node.js | ✅ Production |
| TypeScript | Node.js + ts-node | ✅ Production |
| C | GCC | ✅ Production |
| C++ | G++ | ✅ Production |
| Rust | Rustc | ✅ Production |

---

## Курсы и задачи

### Структура контента

```
Course (Курс)
└── Module (Модуль)
    └── Topic (Тема)
        └── Task (Задача)
```

### Каталог курсов

#### Go Track
| Курс | Задач | Premium | Описание |
|------|-------|---------|----------|
| go-basics | ~50 | Free | Основы Go: синтаксис, типы, функции |
| go-concurrency | ~40 | Premium | Горутины, каналы, sync пакет |
| go-web-apis | ~35 | Premium | HTTP, REST API, middleware |
| go-design-patterns | ~30 | Premium | Паттерны проектирования на Go |
| go-production | ~25 | Premium | Тестирование, профилирование, деплой |

#### Java Track
| Курс | Задач | Premium | Описание |
|------|-------|---------|----------|
| java-core | ~60 | Free | Синтаксис, ООП, коллекции |
| java-modern | ~45 | Premium | Stream API, Optional, лямбды |
| java-advanced | ~40 | Premium | Многопоточность, IO, сеть |
| java-design-patterns | ~35 | Premium | Паттерны проектирования |

#### Python Track
| Курс | Задач | Premium | Описание |
|------|-------|---------|----------|
| python-fundamentals | ~50 | Free | Основы Python |
| python-ml-fundamentals | ~40 | Premium | NumPy, Pandas, ML основы |
| python-deep-learning | ~35 | Premium | TensorFlow, PyTorch |
| python-llm | ~30 | Premium | LLM, RAG, fine-tuning |

#### Cross-Language
| Курс | Задач | Premium | Описание |
|------|-------|---------|----------|
| algo-fundamentals | ~50 | Free | Алгоритмы и структуры данных |
| software-engineering | ~40 | Premium | Software design, architecture |
| prompt-engineering | ~35 | Premium | Prompt Engineering с AI Judge |

### Атрибуты задачи

```typescript
interface Task {
  slug: string;           // Уникальный идентификатор
  title: string;          // Название (EN)
  type: 'CODE' | 'PROMPT'; // Тип задачи
  difficulty: 'easy' | 'medium' | 'hard';
  tags: string[];         // Теги для фильтрации
  estimatedTime: string;  // '15m', '30m', '1h'
  isPremium: boolean;     // Требует подписки
  description: string;    // Описание задачи
  initialCode: string;    // Начальный код
  solutionCode: string;   // Эталонное решение
  testCode: string;       // Код тестов (10 тестов)
  hint1: string;          // Подсказка уровень 1
  hint2: string;          // Подсказка уровень 2
  whyItMatters: string;   // Практическая ценность
  videoUrl?: string;      // YouTube видео-решение
  translations: {         // Переводы
    ru: TranslatedTask;
    uz: TranslatedTask;
  };
}
```

### Сложность задач

| Уровень | XP | Примерное время | Цвет |
|---------|-----|-----------------|------|
| Easy | 25 | 10-15 мин | Зелёный |
| Medium | 50 | 20-40 мин | Жёлтый |
| Hard | 100 | 45-90 мин | Красный |

---

## Рабочее пространство задач

### Desktop Layout (≥768px)

```
┌─────────────────────────────────────────────────────────────┐
│ Header: Course Title | Task Selector | Run | Submit | Nav  │
├──────────────────────────┬──────────────────────────────────┤
│                          │                                  │
│   Description Panel      │     Code Editor (Monaco)         │
│   ──────────────────     │                                  │
│   [Tabs: Description,    │     Language: Go/Java/Python     │
│    Results, Solution,    │                                  │
│    AI Tutor]             │     Resizable width              │
│                          │                                  │
│   Resizable width        │                                  │
│   (drag handle)          │                                  │
│                          │                                  │
└──────────────────────────┴──────────────────────────────────┘
```

### Mobile Layout (<768px)

```
┌────────────────────────┐
│ Header: Title | Menu   │
├────────────────────────┤
│ [Tab Bar]              │
│ Task | Code | AI       │
├────────────────────────┤
│                        │
│ Active Tab Content     │
│                        │
│                        │
├────────────────────────┤
│ Run | Submit           │
└────────────────────────┘
```

### Компоненты

#### Панель описания
- **Описание задачи** — условие с форматированием Markdown
- **Примеры** — Input/Output примеры
- **Подсказки** — 2 уровня (hint1, hint2), открываются последовательно
- **Why It Matters** — зачем это нужно в реальной работе
- **Video Solution** — embedded YouTube видео

#### Панель результатов
- **Test Results** — результаты тестов с Expected/Actual
- **STDOUT** — вывод программы
- **STDERR** — ошибки компиляции/выполнения
- **Статус** — Passed/Failed/Error

#### Панель решения (Premium)
- **Solution Code** — полный код решения
- **Explanation** — пошаговое объяснение решения

#### AI Tutor панель (Premium)
- **Chat Interface** — диалог с AI
- **Context-aware** — AI знает код и условие задачи
- **Rate Limited** — лимиты по подписке

### Горячие клавиши

| Комбинация | Действие |
|------------|----------|
| `Ctrl+Enter` / `Cmd+Enter` | Run Code |
| `Ctrl+Shift+Enter` | Submit |
| `Alt+←` | Previous Task |
| `Alt+→` | Next Task |
| `Ctrl+S` | Save to localStorage |

### Responsive Tabs

При ширине панели описания < 420px:
- Tabs переключаются в compact mode (только иконки)
- При наведении показывается tooltip с названием
- При увеличении ширины текст возвращается

---

## Выполнение кода

### Piston API

Practix использует **Piston** — open-source исполнитель кода с поддержкой 50+ языков.

### Режимы выполнения

#### 1. Run Code (Быстрый запуск)
```
POST /api/submissions/run
```
- **Тесты:** 5 быстрых тестов
- **Сохранение:** Не сохраняется в БД
- **Rate Limit:** 5-second cooldown
- **Результат:** Кэшируется на 5 сек для повторного показа

#### 2. Submit (Полная отправка)
```
POST /api/submissions
```
- **Тесты:** 10 тестов
- **Сохранение:** Сохраняется в БД
- **Rate Limit:** 10 в минуту
- **Результат:** XP начисляется при успехе

### Лимиты выполнения

| Параметр | Значение |
|----------|----------|
| Compile timeout | 15 сек (Java) |
| Run timeout | 10 сек |
| Memory | 256 MB |
| Output size | 65535 bytes |

### Парсинг результатов тестов

Каждый язык имеет свой формат тестов:

**Go:**
```go
// Test1: Description
if got != expected {
    t.Errorf("Expected %v, got %v", expected, got)
}
```

**Java:**
```java
@Test
public void test1_Description() {
    assertEquals(expected, actual);
}
```

**Python:**
```python
def test_1_description():
    assert actual == expected, f"Expected {expected}, got {actual}"
```

### Очередь задач (BullMQ)

```
┌─────────┐     ┌─────────────────┐     ┌────────┐
│ Submit  │ --> │ CODE_EXECUTION  │ --> │ Piston │
│ Request │     │     QUEUE       │     │  API   │
└─────────┘     └─────────────────┘     └────────┘
                      │
                      ▼
                ┌─────────────┐
                │  Processor  │
                │ concurrency │
                │     = 4     │
                └─────────────┘
```

**Характеристики:**
- Concurrency: 4 параллельных задачи
- Retry: 3 попытки с экспоненциальной задержкой
- Dead Letter Queue: неуспешные задачи
- Idempotency: защита от дубликатов

---

## AI Tutor

### Обзор

AI Tutor — интегрированный помощник на базе **Google Gemini 2.0 Flash**, который помогает студентам понять задачи и исправить ошибки.

### Возможности

1. **Объяснение концепций** — AI объясняет теорию
2. **Анализ кода** — AI видит текущий код студента
3. **Подсказки** — контекстные подсказки без готового решения
4. **Debugging** — помощь в поиске ошибок
5. **Multi-language** — отвечает на языке интерфейса (EN/RU/UZ)

### API Endpoint

```
POST /api/ai/ask-tutor
```

**Request:**
```json
{
  "taskId": "go-basics-variables",
  "code": "func main() { ... }",
  "question": "Why does this not compile?",
  "language": "go",
  "uiLanguage": "ru"
}
```

**Response:**
```json
{
  "response": "Ваш код не компилируется, потому что...",
  "tokensUsed": 150
}
```

### Лимиты использования

| Тип подписки | Лимит в день |
|--------------|--------------|
| Free (без подписки) | 5 запросов |
| Course subscription | 30 запросов |
| Global Premium | 100 запросов |
| Prompt Engineering курс | 100 запросов |

### Технические детали

- **Модель:** Gemini 2.0 Flash
- **Max response:** 250 слов
- **Max code in response:** 3 строки
- **Context:** Задача + код студента + история чата
- **Daily reset:** 00:00 UTC

---

## Prompt Engineering задачи

### Обзор

Задачи типа `PROMPT` используют AI-as-Judge для оценки промптов. Студент пишет промпт, AI оценивает его по рубрике и тестовым сценариям.

### UI компонент

```
┌─────────────────────────────────────┐
│ Prompt Editor Panel                 │
│ ┌─────────────────────────────────┐ │
│ │                                 │ │
│ │  [Large Textarea for Prompt]   │ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
│ Character count: 0/2000             │
│                                     │
│ [Submit Prompt]                     │
└─────────────────────────────────────┘
```

### Процесс оценки

1. Студент пишет промпт
2. Нажимает "Submit Prompt"
3. AI получает: промпт + рубрика + тестовые сценарии
4. AI оценивает каждый сценарий
5. Возвращается общий score и feedback

### API Endpoint

```
POST /api/submissions/prompt
```

**Request:**
```json
{
  "taskId": "pe-summarization-basics",
  "prompt": "Summarize the following text in 3 bullet points...",
  "courseId": "prompt-engineering"
}
```

**Response:**
```json
{
  "id": "submission-id",
  "status": "passed",
  "score": 85,
  "scenarios": [
    {
      "name": "Short text",
      "passed": true,
      "score": 90,
      "feedback": "Good structure..."
    },
    {
      "name": "Technical text",
      "passed": true,
      "score": 80,
      "feedback": "Could be more concise..."
    }
  ],
  "overallFeedback": "Great job! Your prompt..."
}
```

### Rate Limiting

- **Limit:** 5 submissions per minute
- **Cooldown:** 5 seconds between submissions

---

## Roadmap Generator

### Обзор

AI-powered генератор персонализированного учебного плана на основе опыта, целей и интересов студента.

### Wizard Flow

```
┌─────────────────────────────────────────────────────────┐
│ Step 1: Known Languages                                 │
│ [x] Python  [x] JavaScript  [ ] Go  [ ] Java  ...      │
└─────────────────────────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│ Step 2: Years of Experience                             │
│ ( ) 0  ( ) <1  (x) 1-2  ( ) 3-5  ( ) 5+                │
└─────────────────────────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│ Step 3: Interests                                       │
│ [x] Backend  [x] AI/ML  [ ] DevOps  [ ] Frontend  ...  │
└─────────────────────────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│ Step 4: Goal                                            │
│ (x) Find First Job  ( ) Become Senior  ( ) Startup     │
└─────────────────────────────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────┐
│ Step 5: Time Commitment                                 │
│ Hours per week: [15]  Target months: [6]               │
└─────────────────────────────────────────────────────────┘
```

### Генерация вариантов

После wizard AI генерирует 3 варианта roadmap:

1. **Recommended** — оптимальный путь
2. **Balanced** — сбалансированный подход
3. **Efficient** — быстрый путь с фокусом на практику

### Структура Roadmap

```typescript
interface Roadmap {
  variant: 'recommended' | 'balanced' | 'efficient';
  phases: Phase[];
  estimatedTime: string;
  resources: Resource[];
}

interface Phase {
  name: string;
  duration: string;
  steps: Step[];
}

interface Step {
  title: string;
  description: string;
  courses: string[];
  estimatedHours: number;
}
```

### Монетизация

| Действие | Стоимость |
|----------|-----------|
| Первая генерация | Бесплатно |
| Последующие генерации | 1 кредит |
| 1 кредит | ~$0.10 (10k UZS) |

### API Endpoints

```
POST /api/roadmaps/generate        # Генерация вариантов
GET  /api/roadmaps/variants/:id    # Получить варианты из кэша
POST /api/roadmaps/select-variant  # Выбрать вариант
GET  /api/roadmaps/:userId         # Текущий roadmap
DELETE /api/roadmaps/:userId       # Удалить roadmap
```

### Кэширование

- **Redis:** `roadmap:variants:{userId}`
- **TTL:** 24 часа
- **Очистка:** При выборе варианта или удалении

---

## Playground (Web IDE)

### Обзор

Свободная среда для написания и выполнения кода без привязки к задачам.

### Возможности

1. **8 языков** — Go, Java, Python, JS, TS, C, C++, Rust
2. **Code Templates** — начальные шаблоны для каждого языка
3. **Stdin Support** — ввод данных
4. **No Account Required** — работает без регистрации
5. **Rate Limiting** — защита от спама

### UI

```
┌─────────────────────────────────────────────────────────┐
│ Playground                              [Language ▼]    │
├──────────────────────────┬──────────────────────────────┤
│                          │                              │
│   Monaco Code Editor     │   Results Panel              │
│                          │   ─────────────              │
│                          │   STDOUT:                    │
│                          │   Hello, World!              │
│                          │                              │
│                          │   STDERR:                    │
│                          │   (empty)                    │
│                          │                              │
├──────────────────────────┴──────────────────────────────┤
│ [Run Code]                                   Timer: 0s  │
└─────────────────────────────────────────────────────────┘
```

### Rate Limits

| Тип пользователя | Cooldown |
|------------------|----------|
| Free | 10 секунд |
| Premium | 5 секунд |

### API Endpoint

```
POST /api/submissions/run
```

**Request:**
```json
{
  "code": "print('Hello')",
  "language": "python",
  "stdin": ""
}
```

---

## Система гамификации

### XP (Experience Points)

| Действие | XP |
|----------|-----|
| Easy task | +25 |
| Medium task | +50 |
| Hard task | +100 |
| First submission bonus | +10 |

### Уровни

| Уровень | XP требуется | Накопительно |
|---------|--------------|--------------|
| 1 | 0 | 0 |
| 2 | 100 | 100 |
| 3 | 150 | 250 |
| 4 | 250 | 500 |
| 5 | 350 | 850 |
| ... | ... | ... |
| 20 | 10000 | 60000 |
| 21+ | +10000 | - |

### Streaks

- **Механизм:** Ежедневная активность (хотя бы 1 submission)
- **Current Streak:** Текущая серия дней подряд
- **Max Streak:** Рекордная серия
- **Reset:** Если пропущен день

### Leaderboard

```
┌──────┬──────────────────┬───────┬────────┬────────┬─────────┐
│ Rank │ Name             │ Level │ XP     │ Streak │ Solved  │
├──────┼──────────────────┼───────┼────────┼────────┼─────────┤
│ 🥇 1 │ Alice            │ 15    │ 45,000 │ 42     │ 312     │
│ 🥈 2 │ Bob              │ 14    │ 42,500 │ 38     │ 298     │
│ 🥉 3 │ Charlie          │ 13    │ 40,000 │ 35     │ 285     │
│ 4    │ Diana            │ 12    │ 35,000 │ 28     │ 250     │
│ ...  │ ...              │ ...   │ ...    │ ...    │ ...     │
└──────┴──────────────────┴───────┴────────┴────────┴─────────┘
```

- **Top 50** пользователей по XP
- **Сортировка:** XP DESC, Streak DESC
- **Кэширование:** Redis, 5 минут TTL

### Badges (в разработке)

| Badge | Условие |
|-------|---------|
| First Blood | Первая решённая задача |
| Problem Solver | 10 решённых задач |
| Marathoner | 7-дневный streak |
| Centurion | 100 решённых задач |

---

## Подписки и платежи

### Типы подписок

#### Global Premium
- **Цена:** ~100 UZS/месяц
- **Доступ:** Все курсы
- **AI Tutor:** 100 запросов/день
- **Queue Priority:** High (1)
- **Code Cooldown:** 5 сек

#### Course Subscription
- **Цена:** ~30-50 UZS/месяц
- **Доступ:** Один курс
- **AI Tutor:** 30 запросов/день
- **Queue Priority:** Medium (5)

#### Free Tier
- **Цена:** Бесплатно
- **Доступ:** Free задачи
- **AI Tutor:** 5 запросов/день
- **Queue Priority:** Low (10)
- **Code Cooldown:** 10 сек

### Платёжные провайдеры

#### Payme
- **Протокол:** JSON-RPC 2.0
- **Валюта:** UZS
- **Webhook:** `POST /api/payments/webhook/payme`

#### Click
- **Протокол:** Webhook с HMAC signature
- **Валюта:** UZS
- **Webhook:** `POST /api/payments/webhook/click`

### Checkout Flow

```
┌───────────┐     ┌──────────────┐     ┌─────────────┐
│  Select   │ --> │   Payment    │ --> │   Webhook   │
│   Plan    │     │   Provider   │     │  Received   │
└───────────┘     └──────────────┘     └─────────────┘
                        │                     │
                        ▼                     ▼
                  ┌──────────────┐     ┌─────────────┐
                  │   Redirect   │     │   Update    │
                  │  to Payment  │     │   Status    │
                  └──────────────┘     └─────────────┘
```

### Grace Period

- **Длительность:** 3 дня после истечения
- **Функционал:** Полный доступ сохраняется
- **Уведомление:** Email за 3 дня до истечения

---

## Личный кабинет

### Dashboard

```
┌─────────────────────────────────────────────────────────┐
│ Welcome back, Alice!                    Level 12 ▓▓▓▓░ │
├──────────────────────────┬──────────────────────────────┤
│ Stats                    │ Weekly Activity              │
│ ─────                    │ ───────────────              │
│ Solved: 156              │ [Bar Chart: Mon-Sun]         │
│ Submissions: 423         │                              │
│ Streak: 14 days          │                              │
│ XP: 12,500               │                              │
├──────────────────────────┼──────────────────────────────┤
│ Recent Submissions       │ Course Progress              │
│ ──────────────────       │ ───────────────              │
│ go-basics: Passed        │ Go Basics: 75% ▓▓▓░          │
│ java-core: Failed        │ Java Core: 45% ▓▓░░          │
│ algo: Passed             │ Algorithms: 30% ▓░░░         │
└──────────────────────────┴──────────────────────────────┘
```

### My Tasks

- **Started Courses** — список начатых курсов
- **Progress** — прогресс по каждому курсу
- **Continue** — быстрый доступ к последней задаче
- **Sorting** — по последнему доступу

### Settings

- **Profile:** Email, имя, аватар
- **Editor:** Размер шрифта, minimap, тема
- **Notifications:** Email digest, новые курсы
- **Language:** EN/RU/UZ
- **Theme:** Light/Dark

---

## Админ панель

### Доступ

- **URL:** `/admin`
- **Требования:** `role === 'ADMIN'`
- **Guard:** AdminGuard (JWT + role check)

### Аналитика

#### Dashboard
- Total Users
- New Users (today/week/month)
- Active Users (24h)
- Total Submissions

#### Courses
- Popularity ranking
- Completion rates
- Average progress

#### Tasks
- Hardest tasks (by fail rate)
- Most popular tasks
- Average attempts

#### Submissions
- By status (passed/failed/error)
- Daily trends
- Peak hours

#### Subscriptions
- Active subscriptions
- Revenue (monthly)
- Churn rate

#### AI Usage
- Daily usage trends
- Average per user
- By subscription tier

### Визуализация

- **Charts:** Recharts (LineChart, BarChart, PieChart)
- **Tables:** Sortable, paginated
- **Filters:** Date range, course, status

---

## Локализация

### Поддерживаемые языки

| Код | Язык | Статус |
|-----|------|--------|
| en | English | ✅ Default |
| ru | Русский | ✅ Full |
| uz | Ўзбек | ✅ Full |

### Механизм

#### Frontend
```tsx
const { language, setLanguage, t } = useLanguage();

// Применение перевода
const translatedTask = t(task);
console.log(translatedTask.title); // Локализованный заголовок
```

#### Backend
```typescript
// Prisma schema
model Task {
  title       String
  description String
  translations Json? // { ru: {...}, uz: {...} }
}
```

### Переводимые поля

- Task: title, description, hints, solution explanation, whyItMatters
- Course: title, description
- Module: title, description
- Topic: title, description
- UI messages: через useUITranslation hook

### Хранение

- **localStorage:** `practix-language`
- **Default:** English (en)
- **Fallback:** Если перевод отсутствует → English

---

## Безопасность

### Аутентификация

| Механизм | Описание |
|----------|----------|
| JWT | JSON Web Tokens для API |
| bcrypt | Хэширование паролей |
| Session Enforcement | Один активный сеанс на пользователя |

### Rate Limiting

| Endpoint | Лимит |
|----------|-------|
| Global | 3 req/sec |
| Submissions | 10 req/min |
| Prompt | 5 req/min |
| Admin | 30 req/min |
| Playground | Custom (10s/5s) |

### Защита от атак

- **SQL Injection:** Prisma ORM с параметризованными запросами
- **XSS:** React auto-escaping
- **CSRF:** SPA architecture + JWT
- **Code Injection:** Piston sandbox + Code Scanner
- **IP Banning:** Автоматическая блокировка подозрительных IP

### Webhook Security

- **Payme:** Basic Auth verification
- **Click:** HMAC-MD5 signature validation
- **IP Whitelist:** Рекомендуется настроить

### Guards

| Guard | Назначение |
|-------|------------|
| JwtAuthGuard | Требует валидный JWT |
| OptionalJwtAuthGuard | JWT опционален |
| AdminGuard | Требует role === 'ADMIN' |
| PlaygroundThrottlerGuard | Custom rate limit |

---

## Технический стек

### Frontend

| Технология | Версия | Назначение |
|------------|--------|------------|
| React | 18.x | UI Framework |
| TypeScript | 5.x | Type Safety |
| Vite | 5.x | Build Tool |
| Tailwind CSS | 3.x | Styling |
| Monaco Editor | - | Code Editor |
| Recharts | - | Charts |
| React Router | 6.x | Routing |

### Backend

| Технология | Версия | Назначение |
|------------|--------|------------|
| NestJS | 10.x | Backend Framework |
| TypeScript | 5.x | Type Safety |
| Prisma | 5.x | ORM |
| PostgreSQL | 15.x | Database |
| Redis | 7.x | Cache + Queue |
| BullMQ | - | Job Queue |
| Piston API | - | Code Execution |
| Gemini API | 2.0 | AI Services |

### DevOps

| Технология | Назначение |
|------------|------------|
| Docker | Containerization |
| Docker Compose | Orchestration |
| Prometheus | Metrics |
| Health Checks | Monitoring |

---

## API Reference

### Аутентификация

```
POST /api/auth/register     # Регистрация
POST /api/auth/login        # Вход
POST /api/auth/logout       # Выход
GET  /api/auth/me           # Текущий пользователь
```

### Курсы

```
GET  /api/courses                    # Все курсы
GET  /api/courses/:slug              # Детали курса
GET  /api/courses/:slug/modules/:id  # Модуль
```

### Задачи

```
GET  /api/tasks/:slug        # Детали задачи
GET  /api/tasks/:slug/access # Проверка доступа
```

### Submissions

```
POST /api/submissions              # Submit solution
POST /api/submissions/run          # Quick run
POST /api/submissions/run-tests    # Run tests (5)
POST /api/submissions/prompt       # Submit prompt
GET  /api/submissions/task/:id     # Submissions for task
GET  /api/submissions/run-result/:id  # Last run result
```

### Gamification

```
GET  /api/gamification/leaderboard  # Top 50
GET  /api/gamification/my-stats     # My stats
GET  /api/gamification/my-rank      # My rank
```

### Subscriptions

```
GET  /api/subscriptions/plans       # All plans
GET  /api/subscriptions/user        # My subscriptions
POST /api/subscriptions/subscribe   # Subscribe
POST /api/subscriptions/cancel      # Cancel
```

### Payments

```
POST /api/payments/checkout         # Create checkout
GET  /api/payments/history          # Payment history
GET  /api/payments/roadmap-credits  # Credit balance
```

### Roadmaps

```
POST /api/roadmaps/generate         # Generate variants
GET  /api/roadmaps/variants/:id     # Get variants
POST /api/roadmaps/select-variant   # Select variant
GET  /api/roadmaps/:userId          # Current roadmap
```

### AI

```
POST /api/ai/ask-tutor              # AI Tutor query
```

### Health

```
GET  /api/health            # Full health check
GET  /api/health/live       # Liveness probe
GET  /api/health/ready      # Readiness probe
GET  /api/health/metrics    # Prometheus metrics
```

---

## Сравнение Free vs Premium

| Функция | Free | Course | Global Premium |
|---------|------|--------|----------------|
| Просмотр задач | ✅ | ✅ | ✅ |
| Run Tests (5) | ✅ | ✅ | ✅ |
| Submit (10) | ✅ | ✅ | ✅ |
| Решение | ❌ | ✅ | ✅ |
| AI Tutor | 5/день | 30/день | 100/день |
| Queue Priority | 10 | 5 | 1 |
| Code Cooldown | 10s | 10s | 5s |
| Все курсы | ❌ | 1 курс | ✅ |
| Playground | 10s limit | 10s limit | 5s limit |

---

*Документ обновлён: 2026-01-17*
